package com.hnure.smartlock.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary ───────────────────────────────────────────────────────────────────
val PrimaryBlue      = Color(0xFF0F62FE)   // IBM Blue — same as web #0F62FE
val PrimaryBlueDark  = Color(0xFF0043CE)
val PrimaryBlueDarker = Color(0xFF002D9C)

// ── Backgrounds ──────────────────────────────────────────────────────────────
val BackgroundGray   = Color(0xFFF4F4F4)   // page bg
val CardWhite        = Color(0xFFFFFFFF)   // card surface
val SurfaceLight     = Color(0xFFF4F4F4)   // secondary surface

// ── Text ─────────────────────────────────────────────────────────────────────
val DarkText         = Color(0xFF161616)   // primary text
val MediumText       = Color(0xFF6E7E91)   // secondary text
val SubtleText       = Color(0xFF8D8D8D)

// ── Borders ───────────────────────────────────────────────────────────────────
val BorderGray       = Color(0xFFE0E0E0)
val BorderLight      = Color(0xFFF4F4F4)

// ── Lock States ───────────────────────────────────────────────────────────────
val LightBlue        = Color(0xFFEBF2FF)   // locked bg tint
val BlueBorder       = Color(0xFFBAE0F7)   // locked border

// ── Status ────────────────────────────────────────────────────────────────────
val GreenSuccess     = Color(0xFF198038)   // online / unlocked
val GreenBg          = Color(0xFFDEFBE6)
val GreenBorder      = Color(0xFFA7F0BA)

// ── Error ─────────────────────────────────────────────────────────────────────
val ErrorRed         = Color(0xFFDA1E28)
val ErrorBg          = Color(0xFFFFF1F1)

// ── Role Colors ───────────────────────────────────────────────────────────────
val OwnerBlue        = Color(0xFF0043CE)
val AdminBlue        = Color(0xFF0F62FE)
val MemberGreen      = Color(0xFF198038)
val GuestGray        = Color(0xFF6E7E91)
